services:
  worker:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - db-data:/app/data
    environment:
      - DB_PATH=/app/data/premiumkreditkarte.db
      - BIKE_DB_PATH=/app/data/fahrradwerkstatt.db

volumes:
  db-data:
